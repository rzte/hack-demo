# Workspace Runtime Migration

该项目用于验证新版 runtime 的 workspace 兼容性。

## Validation Workflow

在生成迁移摘要前，建议先确认当前 workspace runtime 状态是否正常。

常见检查项：

- workspace manifest
- runtime metadata
- active runtime process
- environment consistency

完成后继续生成迁移结果。
